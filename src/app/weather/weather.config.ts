export const WEATHER_CONFIG = {
  // Provide your OpenWeatherMap API key here to fetch real data.
  // For tests or when not set, the service will fall back to stubbed data.
  OPENWEATHER_API_KEY: '',

  // Toggle to enable reverse geocoding (Nominatim). Default true for real browsing.
  ENABLE_REVERSE_GEOCODE: true
};
